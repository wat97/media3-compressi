# Benchmark Template

Record each manual run with:

- Device model
- Android version / API
- Input codec / resolution / fps / bitrate / duration / size
- Chosen policy codec / target height / target bitrate
- Output codec / resolution / bitrate / duration / size
- Compression time
- Result: success / fallback / failed
- Visible artifact notes

